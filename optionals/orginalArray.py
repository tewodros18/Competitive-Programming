class Solution:
    def findOriginalArray(self, changed):
        def chc(a):
            
            return a
            
        changed.sort(key=chc)
        return changed
print(Solution().findOriginalArray([1,3,4,2,6,8]))